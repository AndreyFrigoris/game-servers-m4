# -----------------------------------------------------------------------------
# Private / this machine only — never publish
# -----------------------------------------------------------------------------
AGENTS.md
AGENTS.local.md
*.local.md
.env
.env.*
!.env.example
!**/.env.example

# -----------------------------------------------------------------------------
# Steam dedicated-server binaries (downloaded by SteamCMD, not ours to ship)
# -----------------------------------------------------------------------------
enshrouded-server/EnshroudedServer/
vrising-server/vrising/server/
ark-server/server/
ark-server/.steamcmd/
ark-server/.steam-home/
# Host-only experiment (not a published recipe)
sunkenland-server/

# -----------------------------------------------------------------------------
# Live world data: saves, passwords, logs, backup archives
# -----------------------------------------------------------------------------
**/persistentdata/
**/backups/
**/*.log
**/.TEMP/

# -----------------------------------------------------------------------------
# Local leftovers
# -----------------------------------------------------------------------------
.DS_Store
**/.DS_Store
.metadata_never_index
compose.box64.yml
compose.debug.yml
**/scripts/debug-start.sh

*.swp
.idea/
.vscode/
.cursor/
